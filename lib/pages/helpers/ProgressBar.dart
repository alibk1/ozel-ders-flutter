import 'package:flutter/material.dart';

class LinearIndeterminate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      child: LinearProgressIndicator(),
    );
  }
}
